# Blokir akses langsung ke folder db dari web
# File SQL, migration, dan dokumen tidak boleh diakses via browser
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
