name: Scan Secrets

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  scan-secrets:
    name: Scan Secret (API key / .env / kunci privat)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Jalankan scan-secrets.sh
        run: bash scripts/scan-secrets.sh
