# Blokir akses langsung ke folder hooks dari web
# File git hooks tidak boleh diakses via browser
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
