# Blokir SEMUA akses ke folder logic
# File ini adalah logika bisnis internal — TIDAK boleh diakses via web
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
